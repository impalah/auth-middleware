meta {
  name: Get Token (client credentials)
  type: http
  seq: 3
}

post {
  url: https://granite.auth.eu-central-1.amazoncognito.com/oauth2/token
  body: formUrlEncoded
  auth: none
}

body:form-urlencoded {
  client_id: gc5milbklmehvvbiv32kthd4k
  grant_type: client_credentials
  client_secret: 1snmg6ncvl11erkt1lplaq31u70nv8bou43kgsj7v9kb03tb044a
  redirect_uri: http://localhost:4200
}

script:post-response {
  // Extract the access_token from the response
  let access_token = res.body.access_token;
  
  // Store the access_token in a variable
  bru.setEnvVar("access_token", access_token);
  
}
